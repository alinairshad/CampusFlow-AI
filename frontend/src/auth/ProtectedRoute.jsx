/**
 * ProtectedRoute — checks JWT + role before rendering children.
 * Redirects to /login or /unauthorized as appropriate.
 * Fully implemented in Stage 1.
 */

// Stub: pass-through until Stage 1 wires AuthContext
export default function ProtectedRoute({ children }) {
  return children
}
