[project]
name = "campusflow-ai-backend"
version = "0.1.0"
requires-python = "==3.11.*"

[tool.fastapi]
# Tells FastAPI Cloud where to find the app object.
# Equivalent to: from app.main import app
entrypoint = "app.main:app"
